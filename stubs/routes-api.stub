<?php

use Illuminate\Support\Facades\Route;
use {{NAMESPACE}}\Presentation\Http\Controllers\{{AGGREGATE}}Controller;

Route::prefix('api/{{MODULE_KEBAB}}')
    ->middleware('api')
    ->group(function () {
        Route::apiResource('{{AGGREGATE_KEBAB}}', {{AGGREGATE}}Controller::class);
    });